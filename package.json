{
  "name": "futurize",
  "version": "0.0.0",
  "description": "Turn callback-style functions or promises into futures",
  "main": "index.js",
  "scripts": {
    "test": "mocha --require babel-core/register --reporter dot",
    "prepublish": "npm run transpile",
    "patch-release": "npm version patch && npm publish && git push --follow-tags",
    "minor-release": "npm version minor && npm publish && git push --follow-tags",
    "major-release": "npm version major && npm publish && git push --follow-tags",
    "transpile": "babel src --out-dir .",
    "example": "npm run transpile && babel example --out-dir . && node readFile.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stoeffel/futurize.git"
  },
  "keywords": [
    "future",
    "task",
    "promise",
    "monad"
  ],
  "author": "schtoeffel",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/stoeffel/futurize/issues"
  },
  "homepage": "https://github.com/stoeffel/futurize#readme",
  "dependencies": {
    "data.task": "^3.0.0"
  },
  "devDependencies": {
    "babel": "^6.3.13",
    "babel-cli": "^6.3.17",
    "babel-core": "^6.3.17",
    "babel-preset-es2015": "^6.3.13",
    "babel-preset-stage-0": "^6.3.13",
    "expect": "^1.13.2",
    "mocha": "^2.3.4",
    "pinkie-promise": "^2.0.0"
  }
}
